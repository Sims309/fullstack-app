// backend/jest.global-setup.ts
export default async () => {
  console.log('🚀 [globalSetup] Initialisation des tests...');
  // Tu peux ajouter ici une initialisation spécifique, ex: seeder, migration, etc.
};
